# Railway PostgreSQL connection string.
# Example: postgresql://postgres:password@your-host.railway.app:12345/railway?sslmode=require
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require"

# Optional: set to 1 if you want server-side queries logged in lib/db.ts
LOG_SQL=0
